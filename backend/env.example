# Porta del server
PORT=3007

# LUKSO
LUKSO_RPC_URL=https://rpc.testnet.lukso.network
CONTRACT_ADDRESS=0xE7EFa63E4923626274fbb18319aC4C57377c7BBf
OWNER_UP_ADDRESS=0x83cBE526D949A3AaaB4EF9a03E48dd862e81472C

# Chiavi private (mai committare il .env reale!)
SPONSOR_PRIVATE_KEY=
ORACLE_PRIVATE_KEY=

# Cifratura chiavi utenti — genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# JWT — genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@chainintegrate.it

# Frontend URL (usato per CORS e link nel magic link)
FRONTEND_URL=https://matchpredictor.chainintegrate.it

# football-data.org
FOOTBALL_DATA_API_KEY=
